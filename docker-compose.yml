
services:
  api:
    build: .
    container_name: sf-food-api
    command: uvicorn app.main:app --host 0.0.0.0 --port 8002
    ports:
      - "8002:8002"
    environment:
      SF_FOOD_URL: "https://data.sfgov.org/resource/rqzj-sfat.json"

